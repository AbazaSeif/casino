
;(function($){
  
  if (!(tests instanceof Array)) return;

  // testing game-interface.js

  tests.push({
    module:'game-interface', 
    tests:function() {
      var ret=[];
      
      // TODO
      
      return ret;
    }
  });

})(jQuery);