
;(function($){
  
  if (!(tests instanceof Array)) return;

  // testing bets.js

  tests.push({
    module:'bets', 
    tests:function() {
      var ret=[];
      
      // TODO
      
      return ret;
    }
  });

})(jQuery);